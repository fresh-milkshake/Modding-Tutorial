[project]
name = "modding-tutorial-docs"
version = "0.1.0"
description = "MkDocs configuration for the Slay the Spire 2 modding tutorial."
requires-python = ">=3.12"

[dependency-groups]
docs = [
  "mkdocs>=1.6,<2",
  "mkdocs-material>=9.6,<10",
]
